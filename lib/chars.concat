core #module

## files are parsed only once => defines can be at top level
+char      #define int cast + byte      cast #end
+codepoint #define int cast + codepoint cast #end
charDiff   #define swap int cast swap int cast - #end

?WhiteSpace proc byte => bool :
  #_input_# switch
  ' ' '\r' '\t' '\n' case
    true return
  end-case
  default
    false return
  end
end

#end
core #module
stack #include
## basic procedures/marcos for printing strings

proc  ## string (file)  -> bool
  (file) file =:
  string str =:
  file str 0 str length write
end *->* __IO_fputs =$
fputs #define        core __IO_fputs . ()      #end
## specialized procedures for writing to stdout and stderr addLater? buffering of Output
 puts #define stdout core __IO_fputs . () drop #end
eputs #define stderr core __IO_fputs . () drop #end

#end ## std
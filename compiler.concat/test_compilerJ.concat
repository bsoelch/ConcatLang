test/compiler:

stack #include
core  #import

test proc( int => byte ){ byte cast }

main public proc( => ){
  0x42 test debugPrint
  1 2u '3' u'💻' swap rot3 over dup
  debugPrint
  drop
  debugPrint
  debugPrint
  debugPrint
  debugPrint

  false
  #+cond+# if{
     1
  else false if
     1
  else
     0
  }
  debugPrint
}


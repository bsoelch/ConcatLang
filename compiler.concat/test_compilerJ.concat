test/compiler:

stack #include
core  #import

test proc( int => byte ){ byte cast }

main public proc( => ){
  1 2u '3' u'💻' swap rot3 over dup
  debugPrint
  drop
  debugPrint
  debugPrint
  debugPrint
  debugPrint

  false
  #+cond+# if{
     1
  else false if
     1
  else
     0
  }
  debugPrint

  0x42 test
  dup
    x mut =::
  int y =:
  x debugPrint
  y debugPrint
  x .. debugPrint
  'x' x =
  x .. debugPrint

  true true while{ false rot3 do
    dup debugPrint
  } drop drop
  ' ' debugPrint
  true true while{
    false rot3 dup debugPrint
  do } drop drop
}

